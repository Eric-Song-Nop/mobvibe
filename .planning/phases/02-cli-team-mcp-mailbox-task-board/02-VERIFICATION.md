---
phase: 02-cli-team-mcp-mailbox-task-board
verified: 2026-05-13T17:33:27Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/6
  gaps_closed:
    - "Production ACP MCP callbacks/messages now reach TeamRuntime/TeamMcpRouter through AcpConnection extMethod/extNotification routing and SessionManager-owned TeamRuntime injection."
    - "stdio_bridge is no longer selected while team-bridge-stdio.ts is only a dormant declaration/manifest helper; native MCP-over-ACP is required."
    - "Agent-originated callback-path mailbox/task tool calls now reach durable handlers through the AcpConnection adapter and preserve router-bound caller identity plus content-safe projections."
  gaps_remaining: []
  regressions: []
---

# Phase 2: CLI Team MCP、Mailbox 与 Task Board Verification Report

**Phase Goal:** CLI 本地可以为 team session 提供 `mobvibe_team_*` tools，并把 agent 间消息和任务板持久化为可恢复事实  
**Verified:** 2026-05-13T17:33:27Z  
**Status:** passed  
**Re-verification:** Yes — after 02-07 gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CLI 可以为 team run 启动 team MCP server，并生成 MCP-over-ACP per-session declaration 或安全 per-session bridge config。 | ✓ VERIFIED | Native declaration remains generated by `team-capability.ts:71-77`; team create passes declaration/transport/handlers into `connection.createSession()` at `session-manager.ts:1045-1055`; production connection injects `mcpServers` at `acp-connection.ts:1056-1065` and routes `mcp/connect`, `mcp/message`, `mcp/disconnect` at `acp-connection.ts:1074-1112`. Non-executable bridge fallback is explicitly rejected instead of selected. |
| 2 | 普通非 team session 的 create/load path 不包含 `mobvibe-team` MCP server declaration，也不会修改 agent 全局 MCP 配置。 | ✓ VERIFIED | `buildMcpServers()` returns `[]` without `teamMcpDeclaration` (`acp-connection.ts:1122-1127`); ordinary create/load tests assert `mcpServers: []` (`acp-connection.test.ts:260-310`). No production selection path writes global MCP config. |
| 3 | Backend 不支持 native MCP-over-ACP 且无法安全 bridge 时，用户会得到 team-capable validation error，而不是创建一个不可协作成员。 | ✓ VERIFIED | `resolveTeamMcpTransport()` returns only native `acp` and otherwise throws `CAPABILITY_NOT_SUPPORTED` with `retryable: false` (`team-capability.ts:33-43`, `79-86`); `createTeamSession()` runs selection before `createSession()` (`session-manager.ts:1045-1055`). Focused tests passed. |
| 4 | Agent tool call 可以写入 durable mailbox；message 持久化、read/unread 和 wake status 被分开记录。 | ✓ VERIFIED | Production callback-path test drives `mcp/connect` → list-tools → `mobvibe_team_send_message` through `AcpConnection.handleTeamMcpExtensionMethod()` and then asserts durable projection changes (`acp-connection.test.ts:383-412`, `448-459`). `TeamMcpRouter.handleToolCall()` dispatches to `TeamToolHandlers` with bound caller (`team-mcp-router.ts:76-86`). |
| 5 | Agent tool call 可以创建、列出和更新 durable task board，包含 owner、status 和 blockedBy/blocks。 | ✓ VERIFIED | Callback-path test drives `mobvibe_team_task_create`, `mobvibe_team_task_list`, and `mobvibe_team_task_update` via `mcp/message`, then verifies task counts (`acp-connection.test.ts:414-450`). Durable handlers remain wired through `TeamToolHandlers` → `TaskBoardService`. |
| 6 | Team MCP tools 携带 caller identity，并按 Phase 2 context 的 tool policy 执行。 | ✓ VERIFIED | Caller identity is bound from `mobvibe-team:<agentTeamId>:<memberId>` in `TeamMcpRouter.bindServer()` and not from tool args (`team-mcp-router.ts:104-123`); callback-path test passes spoofed `fromMemberId` but result/projection use bound `leaderMemberId` (`acp-connection.test.ts:403-412`, `451-456`). Phase 2 policy intentionally has no leader-only or confirmation gate for these five tools. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `apps/mobvibe-cli/src/acp/acp-connection.ts` | ACP client callback adapter for `mcp/connect`, `mcp/message` list-tools/tool-call, and `mcp/disconnect` | ✓ VERIFIED | Substantive parser/adapter exists (`parseTeamMcpMessage`, `handleTeamMcpExtensionMethod`); `buildClient()` wires `extMethod`/`extNotification` to it (`acp-connection.ts:196-207`, `720-735`). |
| `apps/mobvibe-cli/src/acp/session-manager.ts` | Production TeamRuntime ownership and router injection into team ACP connections | ✓ VERIFIED | Constructs `AgentTeamStore` + `TeamRuntime` on WAL DB path (`session-manager.ts:254-259`), passes `this.teamRuntime.mcpRouter` into team session creation (`session-manager.ts:1050-1055`), and closes `agentTeamStore` on shutdown (`session-manager.ts:1432-1435`). |
| `apps/mobvibe-cli/src/team/team-capability.ts` | Native-only team MCP session selection until stdio bridge executable exists | ✓ VERIFIED | Only native `capabilities.mcp.acp === true` returns `"acp"`; all other capabilities throw `CAPABILITY_NOT_SUPPORTED` (`team-capability.ts:79-86`). Grep guard found no `return "stdio_bridge"` and no default `team-bridge-stdio.js` executable path. |
| `apps/mobvibe-cli/src/team/team-bridge-stdio.ts` | Dormant reference helper only, not production-selected | ✓ VERIFIED | Still only declares/manifest helper; production selection no longer references it. Manifest input keys now align with supported task args (`team-bridge-stdio.ts:30-49`). |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `acp-connection.ts` | `TeamMcpRouter` | Team MCP callback handlers supplied by SessionManager | ✓ WIRED (manual verification) | `TeamMcpCallbackHandlers` interface matches router methods (`acp-connection.ts:95-107`); `SessionManager` passes `this.teamRuntime.mcpRouter` (`session-manager.ts:1053-1054`); adapter calls `handleConnect`, `handleListTools`, `handleToolCall`, `handleDisconnect` (`acp-connection.ts:1074-1112`). `gsd-sdk verify.key-links` pattern was too narrow and missed the handler-interface indirection. |
| `session-manager.ts` | `team-runtime.ts` | SessionManager-owned TeamRuntime constructed with AgentTeamStore and ordinary session injector | ✓ WIRED | `new TeamRuntime({ store: this.agentTeamStore, sessionManager: this })` at `session-manager.ts:256-259`; `teamRuntime.mcpRouter` injected into team connections at `session-manager.ts:1054`. |
| `team-capability.ts` | `team-bridge-stdio.ts` | stdio bridge fallback declaration | ✓ NOT SELECTED BY DESIGN | `team-capability.ts` imports the dormant declaration type only; production selection does not return `stdio_bridge` and does not default to `./team-bridge-stdio.js`. |
| `TeamMcpRouter` | `TeamToolHandlers` | `handlers.dispatch()` with bound caller | ✓ WIRED | `team-mcp-router.ts:76-86` dispatches using `binding` from server-id parsing, preserving caller/content boundaries. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `AcpConnection` MCP adapter | `ParsedTeamMcpMessage` | `mcp/message` params normalized by `parseTeamMcpMessage()` | Yes | ✓ FLOWING — list-tools and tool-call messages are parsed and routed to handlers (`acp-connection.ts:424-464`, `1087-1103`). |
| `TeamRuntime` / `TeamMcpRouter` | server binding and caller | `mcp/connect` server id `mobvibe-team:<team>:<member>` | Yes | ✓ FLOWING — connect creates binding, list-tools persists readiness, tool-call dispatches with bound caller (`team-mcp-router.ts:34-86`). |
| `MailboxService` path | mailbox row/projection count | `mobvibe_team_send_message` through callback-path test and `AgentTeamStore` SQLite writes | Yes | ✓ FLOWING — callback test observes unread count and source refs while plaintext is absent from summary (`acp-connection.test.ts:397-459`). |
| `TaskBoardService` path | task row/status counts | callback-path task create/list/update through `TeamToolHandlers` and `AgentTeamStore` | Yes | ✓ FLOWING — callback test observes created/listed task id and completed count (`acp-connection.test.ts:414-450`). |
| `team-capability.ts` | transport selection | backend capabilities | Yes | ✓ FLOWING — native ACP yields declaration; non-native/bridge-only rejects before session creation. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Focused Phase 02 callback/durable tests | `pnpm -C apps/mobvibe-cli test -- src/acp/__tests__/acp-connection.test.ts src/acp/__tests__/session-manager.test.ts src/team/__tests__/team-capability.test.ts src/team/__tests__/team-bridge-stdio.test.ts src/team/__tests__/team-mcp-router.test.ts src/team/__tests__/mailbox-service.test.ts src/team/__tests__/task-board-service.test.ts` | `118 pass`, `0 fail`, `300 expect() calls` | ✓ PASS |
| CLI build | `pnpm -C apps/mobvibe-cli build` | `Build complete!` | ✓ PASS |
| CLI lint | `pnpm -C apps/mobvibe-cli lint` | `Checked 73 files in 57ms. No fixes applied.` | ✓ PASS |
| Bridge rejection and wiring guards | `test "$(grep -v '^#' apps/mobvibe-cli/src/team/team-capability.ts | grep -c 'return "stdio_bridge"')" -eq 0 && test "$(grep -v '^#' apps/mobvibe-cli/src/team/team-capability.ts | grep -c 'fileURLToPath(new URL("./team-bridge-stdio.js"')" -eq 0 && test "$(grep -v '^#' apps/mobvibe-cli/src/acp/acp-connection.ts | grep -c 'TeamMcp')" -ge 1` | exit 0 | ✓ PASS |
| Plan artifact verifier | `gsd-sdk query verify.artifacts .planning/phases/02-cli-team-mcp-mailbox-task-board/02-07-PLAN.md --raw` | `all_passed: true`, `3/3` | ✓ PASS |
| Plan key-link verifier | `gsd-sdk query verify.key-links .planning/phases/02-cli-team-mcp-mailbox-task-board/02-07-PLAN.md --raw` | `1/2` due to narrow regex for handler indirection; manual key-link check above verifies intent | ⚠️ TOOL LIMITATION / MANUAL PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| none | Phase plan/summary and conventional probe discovery | No probe files or declared `probe-*.sh` paths for this gap closure | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| MCP-01 | 02-07 | CLI can start/restore team MCP server and expose `mobvibe_team_*` tools | ✓ SATISFIED | TeamRuntime is owned by SessionManager and production ACP callback adapter routes connect/list/call/disconnect to its router. |
| MCP-02 | 02-07 | Team tools injected through ACP per-session declaration | ✓ SATISFIED | Native declaration injected into `mcpServers`; callback/message routing now present. |
| MCP-03 | Regression | Ordinary sessions do not contain `mobvibe-team` | ✓ SATISFIED | Ordinary create/load tests assert `mcpServers: []`; `buildMcpServers()` returns empty absent team declaration. |
| MCP-04 | 02-07 | Non-native fallback only safe per-session bridge; no global config mutation | ✓ SATISFIED | Non-executable stdio bridge is rejected; no global config mutation path found. |
| MCP-05 | Regression | Validate backend support before autonomous member/session creation | ✓ SATISFIED | `buildTeamMcpSessionSelection()` executes before `connection.createSession()` in `createTeamSession()`. |
| MCP-06 | 02-07 | Persist/display MCP readiness phases | ✓ SATISFIED | `mcp/connect` records `tools_waiting`; list-tools records `tools_ready`/`degraded` in `TeamMcpRouter` (`team-mcp-router.ts:34-74`); callback test observes `tools_ready`. |
| MCP-07 | 02-07 | Verified caller identity and tool policy | ✓ SATISFIED | Server-id binding controls caller; spoofed args do not override persisted source refs. |
| COORD-01 | 02-07 | Durable mailbox send message | ✓ SATISFIED | Callback-path send_message test writes durable mailbox metadata/projection. |
| COORD-02 | Regression | Mailbox sender/recipient/read/wake metadata and source refs | ✓ SATISFIED with warning | Metadata and source refs are durable and content-safe. Existing wake retry warning remains non-blocking and outside 02-07 blocker closure. |
| COORD-03 | 02-07 | Durable task board create/list/update with owner/status/deps | ✓ SATISFIED | Callback-path task tests mutate durable task rows and observe status counts. |
| COORD-04 | Regression | No mailbox/task body in Gateway-facing projection | ✓ SATISFIED | Callback-path test serializes summary and verifies mailbox/task plaintext absent. |

No orphaned Phase 2 requirements were found beyond `.planning/REQUIREMENTS.md` rows mapped to Phase 2.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `apps/mobvibe-cli/src/team/team-runtime.ts` | existing review warning | Wake marks messages read before successful injection | ⚠️ Warning | Not part of the 02-07 blocker closure; durable message rows remain recoverable, but retry semantics should be improved in a later reliability slice. |
| `apps/mobvibe-cli/src/acp/acp-connection.ts` | 186, 192, 421, 1013, 1016, 1027, 1124 | Empty return values / arrays | ℹ️ Info | Legitimate ACP empty responses/default args/no team declaration paths; not stubs. |

Debt marker scan over modified ACP/team sources found no blocker `TODO`, `FIXME`, or `XXX` markers in production code. Test mocks contain empty callbacks but are not implementation stubs.

### Human Verification Required

None. The remaining questions are code-level transport wiring and durable-store behavior, both covered by source trace and focused automated tests. Real backend RFD shape changes remain a future compatibility risk, not a current human verification blocker.

### Gaps Summary

No blocking gaps remain. The three previous blockers are closed: production ACP extension callbacks/messages route into the SessionManager-owned TeamRuntime, bridge-only backends are rejected until an executable stdio MCP server exists, and callback-path team tool calls reach durable mailbox/task handlers while preserving caller identity and content-safe projections.

---

_Verified: 2026-05-13T17:33:27Z_  
_Verifier: the agent (gsd-verifier)_
