---
phase: 03-team-run
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02]
files_modified:
  - apps/webui/src/lib/api.ts
  - apps/webui/src/lib/team-store.ts
  - apps/webui/src/hooks/useSocket.ts
  - apps/webui/src/hooks/useSessionQueries.ts
  - apps/webui/src/hooks/useSessionMutations.ts
  - apps/webui/src/hooks/useSessionHandlers.ts
  - apps/webui/src/components/app/CreateSessionDialog.tsx
  - apps/webui/src/lib/ui-store.ts
  - apps/webui/src/i18n/locales/*.json
  - apps/webui/src/hooks/__tests__/useSocket.test.tsx
  - apps/webui/src/hooks/__tests__/useSessionMutations.test.tsx
  - apps/webui/src/hooks/__tests__/useSessionHandlers.test.tsx
autonomous: true
requirements: [ORCH-01, ORCH-02, UI-01]
must_haves:
  truths:
    - "WebUI presents one Create Agent Team flow, but target delivery uses encrypted ordinary session message path."
    - "UI reports create success only after leader target send is accepted."
    - "Target text is never persisted in team-store or sent to Agent Team create route."
---

<objective>
实现 WebUI 创建 Agent Team 的用户流程：用户输入 target、可选 title、backend、cwd/worktree；WebUI 创建 team/leader 后 bootstrap leader E2EE，并通过 existing `sendMessage()` encrypted payload 投递 target。
</objective>

<context>
@.planning/phases/03-team-run/03-CONTEXT.md
@.planning/phases/03-team-run/03-RESEARCH.md
@apps/webui/src/components/app/CreateSessionDialog.tsx
@apps/webui/src/hooks/useSessionHandlers.ts
@apps/webui/src/hooks/useSessionMutations.ts
@apps/webui/src/hooks/useSocket.ts
@apps/webui/src/lib/api.ts
@apps/webui/src/lib/team-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WebUI team projection fetch and socket sync</name>
  <files>apps/webui/src/hooks/useSocket.ts, apps/webui/src/hooks/useSessionQueries.ts, apps/webui/src/lib/team-store.ts, apps/webui/src/hooks/__tests__/useSocket.test.tsx</files>
  <behavior>
    - On startup/machine context, WebUI fetches Agent Teams and replaces projection store.
    - Socket `agent-teams:changed` updates `useTeamStore`.
    - Persisted team store still excludes forbidden keys.
  </behavior>
  <action>Register `gatewaySocket.onAgentTeamsChanged()` in `useSocket` or a small team hook. Add query integration without over-fetching; socket remains primary for live updates.</action>
  <acceptance_criteria>Tests prove socket event updates team store and fetch replaces teams without content-like fields.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend create dialog/state for Agent Team target</name>
  <files>apps/webui/src/components/app/CreateSessionDialog.tsx, apps/webui/src/lib/ui-store.ts, apps/webui/src/i18n/locales/*.json, apps/webui/src/hooks/__tests__/useSessionHandlers.test.tsx</files>
  <behavior>
    - Dialog supports ordinary session mode and Agent Team mode.
    - Team mode shows target textarea and optional title.
    - Existing cwd/backend/worktree UI remains shared and defaults worktree off.
    - Target draft is runtime UI state only and not persisted.
  </behavior>
  <action>Add the smallest mode switch or separate create action needed by existing sidebar/controller. Reuse current worktree controls and request-building logic; avoid duplicating branch detection code.</action>
  <acceptance_criteria>Tests cover opening team create flow with selected workspace, validation for missing target, and worktree request-building parity with ordinary session.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement create team transaction and encrypted target delivery</name>
  <files>apps/webui/src/hooks/useSessionMutations.ts, apps/webui/src/hooks/useSessionHandlers.ts, apps/webui/src/lib/api.ts, apps/webui/src/hooks/__tests__/useSessionMutations.test.tsx</files>
  <behavior>
    - Mutation calls `createAgentTeam()` with metadata/worktree only.
    - On success, it bootstraps leader session E2EE using `leaderSession.wrappedDek`.
    - It sends target via existing `sendMessage()` to `leaderSession.sessionId`.
    - If E2EE status is not `ok`, mutation fails before sending to prevent plaintext Gateway payload.
    - Dialog closes only after target send resolves.
  </behavior>
  <action>Add `createAgentTeamMutation` or a dedicated `createAgentTeamRunMutation`. Use existing `bootstrapSessionE2EE()` and `sendMessage()` semantics. Store the returned leader ordinary session in chat store like createSession success.</action>
  <acceptance_criteria>Tests prove create route body has no target, target send receives encrypted payload when DEK is available, and missing DEK blocks send with app error.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Target plaintext crosses Gateway | Target only travels through `sendMessage()` after E2EE `ok`; otherwise fail. |
| Target persists in team store/local storage | Store only projection; dialog draft is not persisted and team-store stripping remains. |
| User sees success after metadata create but target failed | Mutation closes/reports success only after `sendMessage()` resolves. |
</threat_model>

<verification>
<automated>pnpm -C apps/webui test:run -- src/lib/__tests__/api.test.ts src/lib/__tests__/team-store.test.ts src/hooks/__tests__/useSocket.test.tsx src/hooks/__tests__/useSessionMutations.test.tsx src/hooks/__tests__/useSessionHandlers.test.tsx</automated>
<automated>pnpm -C apps/webui build</automated>
</verification>

<output>
完成后创建 `.planning/phases/03-team-run/03-04-SUMMARY.md`，记录 WebUI create flow、E2EE target delivery 和失败处理。
</output>
