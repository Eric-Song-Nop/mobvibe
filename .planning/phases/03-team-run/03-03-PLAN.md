---
phase: 03-team-run
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - apps/mobvibe-cli/src/team/team-tool-handlers.ts
  - apps/mobvibe-cli/src/team/team-runtime.ts
  - apps/mobvibe-cli/src/team/team-mcp-router.ts
  - apps/mobvibe-cli/src/team/team-bridge-stdio.ts
  - apps/mobvibe-cli/src/acp/acp-connection.ts
  - apps/mobvibe-cli/src/acp/session-manager.ts
  - apps/mobvibe-cli/src/team/__tests__/team-mcp-router.test.ts
  - apps/mobvibe-cli/src/acp/__tests__/acp-connection.test.ts
  - apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts
autonomous: true
requirements: [ORCH-03, ORCH-04, ORCH-05, ORCH-06]
must_haves:
  truths:
    - "Any team member can request spawn through team MCP tooling."
    - "Spawn creates an ordinary member ACP session with team MCP injection."
    - "Spawn failure leaves a failed member slot, not silent rollback."
---

<objective>
把 Phase 2 的 spawn intent 升级为 Phase 3 真实 member session orchestration：新增 `mobvibe_team_spawn_member` 工具，自动创建普通 member ACP session，并继承 leader backend/team-shared checkout defaults。
</objective>

<context>
@.planning/phases/03-team-run/03-CONTEXT.md
@.planning/phases/03-team-run/03-RESEARCH.md
@apps/mobvibe-cli/src/team/team-tool-handlers.ts
@apps/mobvibe-cli/src/team/team-runtime.ts
@apps/mobvibe-cli/src/team/team-mcp-router.ts
@apps/mobvibe-cli/src/acp/session-manager.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add spawn member tool to team MCP surface</name>
  <files>apps/mobvibe-cli/src/team/team-tool-handlers.ts, apps/mobvibe-cli/src/acp/acp-connection.ts, apps/mobvibe-cli/src/team/team-bridge-stdio.ts, apps/mobvibe-cli/src/team/__tests__/team-mcp-router.test.ts, apps/mobvibe-cli/src/acp/__tests__/acp-connection.test.ts</files>
  <behavior>
    - `EXPECTED_TEAM_TOOL_NAMES` includes `mobvibe_team_spawn_member`.
    - `tools_ready` now requires the spawn tool too.
    - Tool args are metadata-only: `name?: string`, `backendId?: string`.
    - Args cannot override caller identity or request per-member worktree.
  </behavior>
  <action>Add the tool name to the parser and expected list. Update tests that enumerate expected tools. Keep dormant bridge manifest aligned if retained.</action>
  <acceptance_criteria>Readiness tests fail when spawn tool is missing; no target/body/prompt fields are accepted for spawn args.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TeamRuntime spawn orchestration service</name>
  <files>apps/mobvibe-cli/src/team/team-runtime.ts, apps/mobvibe-cli/src/team/team-tool-handlers.ts, apps/mobvibe-cli/src/team/__tests__/team-mcp-router.test.ts</files>
  <behavior>
    - Any bound caller can spawn.
    - Missing backend inherits leader backend.
    - Member row is created before session creation so failures have an auditable slot.
    - Successful spawn returns member metadata including `memberId` and `sessionId`.
    - Failed spawn returns structured tool result with `ok: false`, `memberId`, and safe error.
  </behavior>
  <action>Extend TeamToolHandlers services with `spawnMember`. Default TeamRuntime implementation should call a new SessionManager-facing method and emit projection changes after success/failure.</action>
  <acceptance_criteria>Unit tests cover leader-initiated and member-initiated spawn, backend inheritance, backend override validation, and failed slot projection.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create member ordinary session with inherited shared checkout</name>
  <files>apps/mobvibe-cli/src/acp/session-manager.ts, apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts</files>
  <behavior>
    - Member session uses leader execution cwd.
    - Member `workspaceRootCwd`, `worktreeSourceCwd`, and `worktreeBranch` mirror leader/team-shared checkout metadata.
    - Member session gets team MCP injection and appears in ordinary session list.
    - Per-member worktree is not implemented.
  </behavior>
  <action>Add a narrow SessionManager method, e.g. `spawnAgentTeamMember()`, used only by TeamRuntime. It should find leader session metadata, validate backend capability, call `createTeamSession()`, update store, and emit session/team changes.</action>
  <acceptance_criteria>Tests prove spawned member has an ordinary `sessionId`, team MCP declaration, and shared worktree metadata.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Map member turn completion back to TeamRuntime</name>
  <files>apps/mobvibe-cli/src/acp/session-manager.ts, apps/mobvibe-cli/src/team/team-runtime.ts, apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts</files>
  <behavior>
    - When a non-leader team member ordinary session finishes a turn, member lifecycle can move to `completed`.
    - Existing idle notification wake to leader remains CLI-local and projection-safe.
    - Ordinary non-team sessions are unaffected.
  </behavior>
  <action>Add a member lookup by sessionId in AgentTeamStore or SessionManager and call `teamRuntime.onMemberTurnCompleted()` from `recordTurnEnd()` only for team member sessions.</action>
  <acceptance_criteria>Tests prove non-team `recordTurnEnd()` behavior unchanged and team member completion emits team projection update.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Agent spoofs member identity in spawn args | Caller remains bound by `serverId`; ignore/reject identity args. |
| Spawn creates uncontrolled per-member checkout | Phase 3 spawn uses leader/team shared execution cwd only. |
| Failed spawn disappears | Insert member slot before create; update failed lifecycle/health/error on failure. |
| Spawn tool leaks prompt/body | Parser rejects target/body/prompt/content-like args and projection tests serialize result. |
</threat_model>

<verification>
<automated>pnpm -C apps/mobvibe-cli test -- src/team/__tests__/team-mcp-router.test.ts src/acp/__tests__/acp-connection.test.ts src/acp/__tests__/session-manager.test.ts</automated>
<automated>pnpm -C apps/mobvibe-cli build</automated>
</verification>

<output>
完成后创建 `.planning/phases/03-team-run/03-03-SUMMARY.md`，记录 spawn tool 行为、shared checkout 继承和失败 slot 证据。
</output>
