---
phase: 03-team-run
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/mobvibe-cli/src/acp/session-manager.ts
  - apps/mobvibe-cli/src/daemon/socket-client.ts
  - apps/mobvibe-cli/src/team/agent-team-store.ts
  - apps/mobvibe-cli/src/team/team-runtime.ts
  - apps/mobvibe-cli/src/team/team-mcp-router.ts
  - apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts
  - apps/mobvibe-cli/src/daemon/__tests__/socket-client.test.ts
  - apps/mobvibe-cli/src/team/__tests__/agent-team-store.test.ts
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-04, ORCH-05, ORCH-06]
must_haves:
  truths:
    - "CLI Agent Team create creates a leader ordinary ACP session with team MCP injection."
    - "Leader and future members share one execution checkout when team worktree is enabled."
    - "Create result returns only after leader session exists and team MCP is ready or fails with an auditable state."
---

<objective>
把 CLI `rpc:agent-team:create` 从 metadata-only 变成 create/start leader：创建 durable team/member rows，创建 leader ordinary ACP session，注入 team MCP，等待 readiness，更新 projection，并返回 `team + leaderSession`。
</objective>

<context>
@.planning/phases/03-team-run/03-CONTEXT.md
@.planning/phases/03-team-run/03-RESEARCH.md
@.planning/phases/03-team-run/03-PATTERNS.md
@apps/mobvibe-cli/src/acp/session-manager.ts
@apps/mobvibe-cli/src/daemon/socket-client.ts
@apps/mobvibe-cli/src/team/agent-team-store.ts
@apps/mobvibe-cli/src/team/team-runtime.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AgentTeamStore runtime update methods</name>
  <files>apps/mobvibe-cli/src/team/agent-team-store.ts, apps/mobvibe-cli/src/team/__tests__/agent-team-store.test.ts</files>
  <behavior>
    - Store can update team lifecycle/error safely.
    - Store can update member `sessionId`, `lifecycle`, `health`, `error`, `worktreeSourceCwd`, and `worktreeBranch` together.
    - Projection still strips/guards forbidden content keys.
  </behavior>
  <action>Add narrow update methods instead of exposing raw SQL. Preserve existing `updateTeamMemberRuntimeState()` callers or migrate them to the richer method.</action>
  <acceptance_criteria>Tests prove successful leader update and failed leader update both appear in projection without plaintext/body keys.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend createTeamSession for team-shared worktree metadata</name>
  <files>apps/mobvibe-cli/src/acp/session-manager.ts, apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts</files>
  <behavior>
    - Leader creation can receive `worktree` and reuse ordinary worktree creation behavior.
    - Member creation can receive an already resolved execution cwd plus explicit workspace/worktree metadata, without creating another worktree.
    - SessionSummary for team-shared worktree keeps `workspaceRootCwd` and `worktreeSourceCwd` as the source repo root.
  </behavior>
  <action>Extract the ordinary worktree resolution logic into a private helper used by both `createSession()` and `createTeamSession()`, or make `createTeamSession()` accept a small resolved execution context. Avoid broad refactors.</action>
  <acceptance_criteria>Tests cover leader team worktree creation, member reuse of leader worktree cwd, and ordinary session behavior unchanged.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement SessionManager.createAgentTeamRun</name>
  <files>apps/mobvibe-cli/src/acp/session-manager.ts, apps/mobvibe-cli/src/acp/__tests__/session-manager.test.ts</files>
  <behavior>
    - Creates Agent Team row and leader member row.
    - Sets team lifecycle `starting` then `running` on success.
    - Sets leader lifecycle `creating_session` then `running` with `sessionId`.
    - Updates MCP phase through existing TeamMcpRouter callback path and waits for `tools_ready` with a bounded timeout.
    - If backend invalid, MCP unsupported, worktree fails, or session creation fails, returns/throws structured error and leaves failed team/member projection.
  </behavior>
  <action>Add a public `createAgentTeamRun(params)` method on SessionManager. It should own orchestration and use the SessionManager-owned AgentTeamStore/TeamRuntime so MCP callbacks and projection events share one source of truth.</action>
  <acceptance_criteria>Tests prove successful create returns `{ team, leaderSession }`; failure leaves auditable failed state; unsupported backend/MCP rejects before target delivery.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire SocketClient Agent Team create RPC to SessionManager</name>
  <files>apps/mobvibe-cli/src/daemon/socket-client.ts, apps/mobvibe-cli/src/daemon/__tests__/socket-client.test.ts</files>
  <behavior>
    - `rpc:agent-team:create` calls `sessionManager.createAgentTeamRun()` rather than metadata-only store create.
    - RPC success emits `agent-teams:changed` with safe projection and relies on existing session events for leader session visibility.
    - SessionManager Agent Team changed events are relayed by SocketClient for MCP readiness and later mailbox/task changes.
  </behavior>
  <action>Add `sessionManager.onAgentTeamChanged()` subscription in SocketClient setup. Keep route/RPC logs metadata-only.</action>
  <acceptance_criteria>SocketClient tests prove create RPC returns leaderSession and emits team changed; session events remain ordinary `sessions:changed`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Team create creates a session without MCP tools | Wait for `tools_ready` or fail with structured error and failed projection. |
| Worktree creation silently downgrades to current checkout | Worktree failure throws; failed team/member state remains auditable. |
| Projection leaks target or mailbox/task body | Store projection boundary tests serialize returned `team`. |
| Duplicate store instances miss updates | SessionManager owns orchestration and emits Agent Team changed events to SocketClient. |
</threat_model>

<verification>
<automated>pnpm -C apps/mobvibe-cli test -- src/acp/__tests__/session-manager.test.ts src/daemon/__tests__/socket-client.test.ts src/team/__tests__/agent-team-store.test.ts</automated>
<automated>pnpm -C apps/mobvibe-cli build</automated>
</verification>

<output>
完成后创建 `.planning/phases/03-team-run/03-02-SUMMARY.md`，记录 CLI create/start leader 行为、worktree 语义和 MCP readiness 证据。
</output>
