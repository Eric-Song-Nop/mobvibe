---
phase: 03-team-run
plan: 05
type: execute
wave: 5
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - apps/webui/src/app/use-main-app-controller.tsx
  - apps/webui/src/app/MainLayout.tsx
  - apps/webui/src/app/SessionWorkspace.tsx
  - apps/webui/src/components/app/AppSidebar.tsx
  - apps/webui/src/components/session/SessionSidebar.tsx
  - apps/webui/src/components/team/AgentTeamOverview.tsx
  - apps/webui/src/hooks/useSessionList.ts
  - apps/webui/src/lib/workspace-utils.ts
  - apps/webui/src/i18n/locales/*.json
  - apps/webui/src/components/app/__tests__/AppSidebar.test.tsx
  - apps/webui/src/components/team/__tests__/AgentTeamOverview.test.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-04, UI-05, ORCH-04, ORCH-05, ORCH-06]
must_haves:
  truths:
    - "Agent Team appears in the workspace session list as a default-expanded parent row."
    - "Team-owned ordinary sessions are folded under the team by default."
    - "Clicking a member row jumps to that ordinary session."
    - "Team overview shows metadata-only member/MCP/task/mailbox projection."
---

<objective>
交付 Phase 3 最小可观察 UI：workspace session list 中显示 Agent Team parent + member child rows，点击 parent 显示 metadata-only overview，点击 member 复用普通 session UI。
</objective>

<context>
@.planning/phases/03-team-run/03-CONTEXT.md
@.planning/phases/03-team-run/03-PATTERNS.md
@apps/webui/src/components/session/SessionSidebar.tsx
@apps/webui/src/app/SessionWorkspace.tsx
@apps/webui/src/app/use-main-app-controller.tsx
@apps/webui/src/lib/team-store.ts
@apps/webui/src/hooks/useSessionList.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive team-aware session list entries</name>
  <files>apps/webui/src/hooks/useSessionList.ts, apps/webui/src/lib/workspace-utils.ts</files>
  <behavior>
    - Teams filter by selected machine and effective workspace.
    - Worktree team sessions group under `workspaceRootCwd || worktreeSourceCwd || cwd`, preserving source repo grouping.
    - Ordinary sessions whose sessionId appears in a team member are excluded from top-level ordinary list.
  </behavior>
  <action>Add a small derived model for sidebar rendering rather than mutating chat store session types into fake team sessions.</action>
  <acceptance_criteria>Unit tests prove worktree leader/member stay under original workspace and no duplicate child sessions appear at top level.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Agent Team parent and member child rows in SessionSidebar</name>
  <files>apps/webui/src/components/session/SessionSidebar.tsx, apps/webui/src/components/app/AppSidebar.tsx, apps/webui/src/components/app/__tests__/AppSidebar.test.tsx</files>
  <behavior>
    - Agent Team parent row defaults expanded.
    - Parent row shows title, lifecycle/MCP status, compact task/mailbox count badges, and updated time.
    - Child rows show leader/member name, backend, lifecycle, worktree branch, and session link state.
    - Member child click calls ordinary `onSelectSession(sessionId)` when sessionId exists.
  </behavior>
  <action>Keep existing ordinary session item component mostly intact. Add a separate team row component and pass team-aware entries from controller/sidebar props.</action>
  <acceptance_criteria>Tests cover expanded parent, hidden duplicate ordinary sessions, badges, and member click navigation.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add minimal AgentTeamOverview workspace view</name>
  <files>apps/webui/src/app/use-main-app-controller.tsx, apps/webui/src/app/SessionWorkspace.tsx, apps/webui/src/components/team/AgentTeamOverview.tsx, apps/webui/src/components/team/__tests__/AgentTeamOverview.test.tsx</files>
  <behavior>
    - Selecting team parent sets `activeAgentTeamId` without treating team as a session kind.
    - Overview shows title, lifecycle, workspace, members, MCP phase, worktree branch, safe error, task/mailbox counts, and jump buttons.
    - Overview does not render mailbox/task body, target text, summary body, or agent output.
    - Selecting any ordinary session clears active team overview and returns to existing chat UI.
  </behavior>
  <action>Use `useTeamStore.activeAgentTeamId` or controller state. Add `AgentTeamOverview` as a small metadata component in `SessionWorkspace` branch. Do not build rich timeline/chat preview.</action>
  <acceptance_criteria>Tests assert forbidden text fields are absent and jump button calls ordinary session selection.</acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run UI quality checks and update Chinese docs</name>
  <files>docs/ or .planning/phases/03-team-run/03-05-SUMMARY.md</files>
  <behavior>
    - Desktop and mobile sidebar paths remain usable.
    - UI follows existing design language and keeps keyboard/click targets accessible.
    - Chinese docs explain create flow, target E2EE delivery, team-shared worktree, and member navigation.
  </behavior>
  <action>Run focused webui tests/build, then run React Best Practices and Web Design Guideline review as required by repo instructions for WebUI changes. Record findings/fixes in summary.</action>
  <acceptance_criteria>Summary includes tests/build and UI review evidence or explicit residual risks.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Agent Team modeled as fake ordinary session | Keep separate `activeAgentTeamId`/team entries; ordinary session store remains unchanged. |
| Projection UI leaks content | Render only typed projection fields and counts; tests assert body/content keys absent. |
| User loses member ordinary session navigation | Child rows call existing ordinary session activation path. |
| Worktree team appears as separate workspace | Preserve existing source repo grouping rule. |
</threat_model>

<verification>
<automated>pnpm -C apps/webui test:run -- src/components/app/__tests__/AppSidebar.test.tsx src/components/team/__tests__/AgentTeamOverview.test.tsx src/hooks/__tests__/useSessionList.test.tsx</automated>
<automated>pnpm -C apps/webui build</automated>
<automated>pnpm -C apps/webui lint</automated>
</verification>

<output>
完成后创建 `.planning/phases/03-team-run/03-05-SUMMARY.md`，记录最小 UI 行为、可访问性/设计检查、使用方法和残余风险。
</output>
