---
phase: 03-team-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/agent-team.ts
  - packages/shared/src/types/socket-events.ts
  - apps/gateway/src/routes/agent-teams.ts
  - apps/gateway/src/routes/sessions.ts
  - apps/gateway/src/services/team-router.ts
  - apps/gateway/src/routes/__tests__/agent-teams.test.ts
  - apps/webui/src/lib/api.ts
  - apps/webui/src/lib/__tests__/api.test.ts
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-04, ORCH-06]
must_haves:
  truths:
    - "Agent Team create RPC carries worktree metadata but never target plaintext."
    - "Create result can return leader SessionSummary metadata so WebUI can bootstrap E2EE before target delivery."
    - "Gateway route keeps forbidden plaintext/secret-like key rejection."
---

<objective>
建立 Phase 3 create/start contract：Agent Team create request 支持 team-shared worktree 参数，response 支持 leader ordinary session metadata，但 target 仍通过 ordinary encrypted message path 投递，不能进入 Agent Team route。
</objective>

<context>
@.planning/phases/03-team-run/03-CONTEXT.md
@.planning/phases/03-team-run/03-RESEARCH.md
@.planning/phases/03-team-run/03-PATTERNS.md
@packages/shared/src/types/agent-team.ts
@packages/shared/src/types/socket-events.ts
@apps/gateway/src/routes/agent-teams.ts
@apps/gateway/src/routes/sessions.ts
@apps/webui/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared Agent Team create contract without target plaintext</name>
  <files>packages/shared/src/types/agent-team.ts, packages/shared/src/types/socket-events.ts</files>
  <behavior>
    - `CreateAgentTeamRpcParams` accepts `worktree?: CreateSessionWorktreeOptions`.
    - `CreateAgentTeamRpcResult` accepts `leaderSession?: SessionSummary`.
    - No shared Agent Team create type introduces fields named `target`, `prompt`, `content`, `body`, or `description`.
  </behavior>
  <action>Import/reuse `CreateSessionWorktreeOptions` and `SessionSummary` in shared types. Keep `workspaceMode` as existing `TeamWorkspaceMode`; Phase 3 uses `shared_workspace` plus optional `worktree` to represent team-shared worktree.</action>
  <acceptance_criteria>Shared compile succeeds; contract clearly separates Agent Team metadata from encrypted ordinary session target delivery.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse Agent Team worktree params in Gateway with ordinary session semantics</name>
  <files>apps/gateway/src/routes/agent-teams.ts, apps/gateway/src/routes/sessions.ts, apps/gateway/src/routes/__tests__/agent-teams.test.ts</files>
  <behavior>
    - `/acp/agent-teams` accepts `worktree: { sourceCwd, branch?, baseBranch?, relativeCwd? }` with the same relative path and branch validation as `/acp/session`.
    - Forbidden plaintext keys still fail before CLI forwarding.
    - Route logs do not include request body, target, branch args beyond safe metadata already logged elsewhere.
  </behavior>
  <action>Extract or duplicate minimally the existing `normalizeRelativeCwd` and branch validation from session route. Forward only allowlisted create fields to `TeamRouter.createAgentTeam()`.</action>
  <acceptance_criteria>Agent Team route tests cover valid worktree, absolute/escaping `relativeCwd` rejection, branch starting with `-` rejection, and forbidden target/plaintext field rejection.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align WebUI API payload with the new safe contract</name>
  <files>apps/webui/src/lib/api.ts, apps/webui/src/lib/__tests__/api.test.ts</files>
  <behavior>
    - `CreateAgentTeamPayload` sends `worktree?: CreateSessionWorktreeOptions` shape.
    - `createAgentTeam()` does not serialize local target text.
    - API tests assert forbidden local fields are not sent even if caller passes an extended object.
  </behavior>
  <action>Replace legacy `worktreeSourceCwd`/`worktreeBranch` payload fields with `worktree`. Keep explicit allowlist serialization.</action>
  <acceptance_criteria>WebUI API tests prove Agent Team create payload is metadata/worktree-only.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Target plaintext accidentally sent to Gateway create route | No target field in shared/API contract; route rejects forbidden keys recursively. |
| Worktree path injection | Reuse ordinary session `relativeCwd` normalization and branch flag rejection. |
| Contract ambiguity between workspace and worktree | Keep `workspaceRootCwd` as logical repo workspace; `worktree` only describes optional execution checkout creation. |
</threat_model>

<verification>
<automated>pnpm -C packages/shared build</automated>
<automated>pnpm -C apps/gateway test:run -- src/routes/__tests__/agent-teams.test.ts</automated>
<automated>pnpm -C apps/webui test:run -- src/lib/__tests__/api.test.ts</automated>
</verification>

<output>
完成后创建 `.planning/phases/03-team-run/03-01-SUMMARY.md`，记录 contract 变更、验证命令和 target plaintext 边界结论。
</output>
