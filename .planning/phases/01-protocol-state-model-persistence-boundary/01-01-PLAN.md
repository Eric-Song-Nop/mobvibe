---
phase: 01-protocol-state-model-persistence-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/agent-team.ts
  - packages/shared/src/types/session.ts
  - packages/shared/src/types/errors.ts
  - packages/shared/src/types/socket-events.ts
  - packages/shared/src/index.ts
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-04
  - TEAM-05
  - LIFE-01
autonomous: true
must_haves:
  truths:
    - WebUI、Gateway 和 CLI 使用同一个 shared Agent Team metadata/RPC contract。
    - Agent Team leader/member、MCP readiness、workspace/worktree、activity counts 和 lifecycle 使用 shared 类型表达。
    - Shared Gateway-facing contract 不定义 prompt、agent output、mailbox body、task body/title/description、summary body 或 secret material 字段。
  artifacts:
    - path: packages/shared/src/types/agent-team.ts
      provides: Agent Team shared contract, lifecycle unions, source refs, counts, RPC payloads, and projection types
    - path: packages/shared/src/types/socket-events.ts
      provides: typed Agent Team RPC and projection event contracts
  key_links:
    - from: packages/shared/src/types/socket-events.ts
      to: packages/shared/src/types/agent-team.ts
      via: Agent Team RPC params/results and `AgentTeamsChangedPayload`
      pattern: AgentTeamsChangedPayload
---

## Phase Goal

**As a** Mobvibe 用户，**I want to** 拥有稳定、可恢复、跨 WebUI/Gateway/CLI 一致理解的 Agent Team metadata，**so that** 后续 team runtime 可以安全建立在同一协议、状态模型和内容边界之上。

<objective>
Create the shared Agent Team protocol contract.

Purpose: This prevents protocol drift before CLI/Gateway/WebUI consume Agent Team metadata and locks lifecycle/content boundary semantics in shared types.
Output: Shared types and socket payload contracts proving Agent Team, member, MCP, source-ref, RPC, and content-boundary shapes compile.
</objective>

<execution_context>
@/home/eric/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eric/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-protocol-state-model-persistence-boundary/01-CONTEXT.md
@.planning/phases/01-protocol-state-model-persistence-boundary/01-RESEARCH.md
@.planning/phases/01-protocol-state-model-persistence-boundary/01-PATTERNS.md
@AGENTS.md
@packages/shared/AGENTS.md

<interfaces>
Use the pattern map excerpts for existing contracts:
- `packages/shared/src/types/errors.ts`: `ErrorDetail`, `ErrorCode`, `ErrorScope`, `createErrorDetail`.
- `packages/shared/src/types/socket-events.ts`: `RpcRequest<TParams>`, `RpcResponse<TResult>`, `CliToGatewayEvents`, `GatewayToCliEvents`, `GatewayToWebuiEvents`.
- `packages/shared/src/types/session.ts`: extend `AgentSessionCapabilities` with MCP capability discovery; do not add team lifecycle to `SessionSummary`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared Agent Team contract and socket payloads</name>
  <files>packages/shared/src/types/agent-team.ts, packages/shared/src/types/session.ts, packages/shared/src/types/errors.ts, packages/shared/src/types/socket-events.ts, packages/shared/src/index.ts</files>
  <read_first>packages/shared/src/types/session.ts, packages/shared/src/types/errors.ts, packages/shared/src/types/socket-events.ts, packages/shared/src/index.ts, .planning/phases/01-protocol-state-model-persistence-boundary/01-CONTEXT.md, .planning/phases/01-protocol-state-model-persistence-boundary/01-PATTERNS.md</read_first>
  <behavior>
    - Build assertion: shared exports compile when importing `AgentTeamSummary`, `TeamMemberSummary`, `TeamMcpPhase`, `TeamSourceRef`, `CreateAgentTeamRpcParams`, `CreateAgentTeamRpcResult`, `ListAgentTeamsRpcResult`, `GetAgentTeamRpcParams`, `GetAgentTeamRpcResult`, and `AgentTeamsChangedPayload`.
    - Source assertion: `AgentTeamLifecycle` contains exactly `pending`, `starting`, `running`, `completed`, `failed`, `cancelled`, `archived` and does not contain `idle` or `ready`.
    - Source assertion: `TeamMemberLifecycle` contains exactly `pending`, `creating_session`, `running`, `completed`, `failed`, `cancelled`, `detached`, `archived` and does not contain `idle` or `ready`.
  </behavior>
  <action>Create `packages/shared/src/types/agent-team.ts` with string ID aliases `AgentTeamId`, `TeamMemberId`, `TeamMailboxMessageId`, `TeamTaskId`, `TeamSummaryRefId`; lifecycle unions from 01-CONTEXT lines 113-160; `TeamMcpTransport` values `acp`, `stdio_bridge`, `http_bridge`; `TeamMemberHealth` values `healthy`, `degraded`, `error`; `TeamWorkspaceMode` values `shared_workspace`, `per_member_worktree`; `TeamMailboxWakeStatus` values `not_needed`, `pending`, `sent`, `failed`; `TeamTaskStatus` values `todo`, `in_progress`, `blocked`, `completed`, `failed`, `cancelled`; projection types `AgentTeamSummary`, `TeamMemberSummary`, `TeamMcpStatusSummary`, `TeamMailboxCounts`, `TeamTaskCounts`, `TeamSummaryRef`, and `TeamSourceRef` union exactly covering `session_event`, `member_session`, `mailbox_message`, and `task`. Add RPC payload/result types for `CreateAgentTeamRpcParams`, `CreateAgentTeamRpcResult`, `ListAgentTeamsRpcParams`, `ListAgentTeamsRpcResult`, `GetAgentTeamRpcParams`, `GetAgentTeamRpcResult`, plus `AgentTeamsChangedPayload` with `added`, `updated`, `removed`, and optional `machineId`. Extend `AgentSessionCapabilities` with `mcp?: AgentMcpCapabilities` where `AgentMcpCapabilities` has optional booleans `acp`, `stdio`, and `perSessionBridge`. Extend shared socket event interfaces with `rpc:agent-team:create`, `rpc:agent-teams:list`, `rpc:agent-team:get`, and `agent-teams:changed`. Export all new public types from `packages/shared/src/index.ts`. Use `ErrorDetail` for team errors; do not introduce ad-hoc `{ ok, error: string }` shapes. Do not add Gateway-facing fields named `prompt`, `content`, `body`, `description`, `summaryText`, `agentOutput`, `providerToken`, `masterSecret`, `dek`, or `secret`.</action>
  <acceptance_criteria>
    - Source assertion: `packages/shared/src/types/agent-team.ts` defines the required ID aliases, lifecycle unions, MCP transport/phase types, source refs, projection types, and RPC types.
    - Source assertion: `packages/shared/src/types/session.ts` defines `AgentMcpCapabilities` and `AgentSessionCapabilities.mcp?: AgentMcpCapabilities`.
    - Source assertion: `packages/shared/src/types/socket-events.ts` includes the three Agent Team RPC events and `agent-teams:changed` in typed event interfaces.
    - Source assertion: `grep -v '^#' packages/shared/src/types/agent-team.ts | grep -E 'prompt|content|body|description|summaryText|agentOutput|providerToken|masterSecret|dek|secret'` returns no Gateway-facing plaintext field definitions except comments explicitly stating forbidden terms.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -C packages/shared build</automated>
  </verify>
  <done>Shared contract compiles and makes Agent Team, member, MCP, source refs, RPC payloads, and content boundary types available from `@mobvibe/shared`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared type contract → all packages | Incorrect shared fields can permit downstream plaintext leakage or lifecycle drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-T | Tampering | shared RPC payloads | mitigate | Define create/list/get params/results centrally so downstream plans do not invent lifecycle/MCP truth fields. |
| T-01-03-I | Information Disclosure | `agent-team.ts` shared types | mitigate | Do not define Gateway-facing plaintext fields named prompt/content/body/description/summaryText/agentOutput or secret fields. |
</threat_model>

<verification>
Run `pnpm -C packages/shared build`.
</verification>

<success_criteria>
Agent Team shared types compile, lifecycle/MCP/readiness dimensions are split, RPC payloads are typed, and Gateway-facing shared projection excludes forbidden plaintext and secret fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-state-model-persistence-boundary/01-01-SUMMARY.md`.
</output>
